import 'package:flutter/material.dart';

class web_test extends StatelessWidget {
  const web_test({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("data")),
    );
  }
}
